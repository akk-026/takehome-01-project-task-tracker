{
  "name": "northstar-project-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Project and task tracking take-home application.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
